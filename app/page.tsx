import { ReactElement } from "react";

const Page = ():ReactElement => (
  <div>
    <h1 className="text-3xl font-bold underline">
      Hello world!
      data
    </h1>
  </div>
);

export default Page;
